// Moved to @ever-jobs/models (country-normalize.ts) so the DTO layer can
// canonicalize country values. Re-exported here for existing callers.
export { regionNameFromCode } from '@ever-jobs/models';
